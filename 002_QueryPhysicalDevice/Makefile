include ../Makefile.env

TARGET = 002_QueryPhysicalDevice

SRCS = 
SRCS += main.c

OBJS =
OBJS += $(patsubst %.cc,%.o,$(patsubst %.cpp,%.o,$(patsubst %.c,%.o,$(SRCS))))

INCLUDES +=

CFLAGS   +=

LDFLAGS  +=

include ../Makefile.include
